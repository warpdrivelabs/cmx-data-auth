[package]
name = "cmx-dataauth-server"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "数据权限引擎独立微服务 HTTP 服务器（cmx-web-chassis 骨架）。"

[[bin]]
name = "cmx-dataauth-server"
path = "src/main.rs"

[dependencies]
cmx-dataauth-app.workspace = true
cmx-web-chassis.workspace = true
cmx-web-monitor.workspace = true
cmx-database-pg.workspace = true
cmx-service-base = { workspace = true, features = ["config-manager", "registry-config"] }
axum.workspace = true
tokio.workspace = true
dotenvy.workspace = true
anyhow.workspace = true
tracing.workspace = true

# 路由契约守护测试：oneshot 探测挂载路径（仅测试编译，不进运行时依赖）
[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
